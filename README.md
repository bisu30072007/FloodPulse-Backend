FloodPulse SMS Backend

Upload server.js, package.json, .env.example and .gitignore to GitHub. Do not upload real Twilio secrets.

Render build command: npm install
Render start command: npm start

Add TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN and TWILIO_PHONE_NUMBER in Render Environment.
